Installation Instructions
-------------------------

If you are on Ubuntu (>= 14.04), run sudo_setup.sh script in the qsim
directory. Otherwise run setup.sh This will set up the entire QSim
infrastructure. On other distributions, comment the apt-get lines,
install build dependencies for QEMU and run the script.

$ ./setup.sh

Please note that download OS images needs atleast 10G space.
